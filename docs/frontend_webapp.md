# Frontend component edits

## Login page
1. Change "Register for Node Initialization" to "New user? Register now."
2. Make the Setting up the "Business Fundamentals" and "The Document Vault" to optional after logging in. Provide a button that allows the user to proceed to the dashboard, then selectively setup the SME profile later.

## Dashboard (Home)
1. In the "Upcoming Deadlines", don't put the missing criteria, but put the deadlines instead.
2. 

# Frontend-backend integration

1. Auth key setting (can use Clerk framework)
2. Store questionnaire responses into the database